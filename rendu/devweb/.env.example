# BFF (Fastify) — see server/config.ts
# All values below are the defaults the server falls back to.

# Use the internal IP http://51.161.42.245:11434 only if your IP is whitelisted in UFW.
OLLAMA_URL=https://ollama.velha-tech.fr

# Model name exposed by the INFRA deployment
OLLAMA_MODEL=phi3-financial-optimized

# BFF listen port and host
PORT=3001
HOST=127.0.0.1
